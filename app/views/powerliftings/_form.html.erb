<%= form_for(@powerlifting) do |f| %>
  <% if @powerlifting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@powerlifting.errors.count, "error") %> prohibited this powerlifting from being saved:</h2>

      <ul>
      <% @powerlifting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :move %><br>
    <%= f.text_field :move %>
  </div>
  <div class="field">
    <%= f.label :result %><br>
    <%= f.text_field :result %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
